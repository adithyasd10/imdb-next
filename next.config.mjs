/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'i.pinimg.com', 'github.com', 'raw.githubusercontent.com', 'image.tmdb.org'],
  },
};

export default nextConfig;
